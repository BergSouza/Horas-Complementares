import {StyleSheet} from 'react-native';

const style = StyleSheet.create({
    button: {
        paddingVertical: 10,
        marginHorizontal: 10,
        marginTop: 10,
    },
});

export default style;